<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <EditTextPreference
        android:key="base_url"
        android:title="API base url" />
    <EditTextPreference
        android:key="solr_url"
        android:title="SOLR geocoding url"/>
    <EditTextPreference
        android:key="map_rtes"
        android:hint="must be comma separated route ids"
        android:title="Route IDs for Map"/>
    <EditTextPreference
        android:key="long_tabs"
        android:hint="must be comma separated list of types [origin,destination,on_offf,transfers]"
        android:title="Tabs for long survey"/>
    <EditTextPreference
        android:key="long_headers"
        android:hint="must be comma separated list of headers for long survey tabs"
        android:title="Long survey headers"/>
</PreferenceScreen>